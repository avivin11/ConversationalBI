# ============================================================
# RAG Conversational BI Assistant — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
#
# Package groups explained:
#   LangChain family  → orchestration, SQL agent, LLM wrappers
#   ChromaDB          → local vector store (stores embeddings)
#   Sentence-transformers → turns text into embedding vectors (free, CPU)
#   Streamlit         → web UI framework
#   Groq              → fast free LLM API client
#   SQLAlchemy        → database connection layer (SQLite / Postgres / MySQL)
#   python-dotenv     → loads .env file into environment variables

# --- LangChain (split-package pattern as of 2025) ---
# langchain         → core abstractions (chains, prompts, agents)
# langchain-community → SQL agent, document loaders, community tools
# langchain-groq    → Groq LLM wrapper for LangChain
# langchain-huggingface → HuggingFace embeddings wrapper for LangChain
langchain==1.3.11
# langchain-community==0.3.24
langchain-groq==0.3.2
langchain-huggingface==0.2.0

# --- Vector store ---
chromadb==1.5.9

# --- Embeddings model (downloads ~80MB on first use, not on install) ---
sentence-transformers==5.6.0

# --- UI ---
streamlit==1.45.0

# --- LLM API clients ---
groq==0.37.1

# --- Database layer ---
sqlalchemy==2.0.40

# --- Data handling ---
pandas==2.2.3

# --- Environment / config ---
python-dotenv==1.1.0

# --- Optional: Gemini backup LLM ---
# Uncomment if you want to use Gemini as fallback
# langchain-google-genai==2.1.4
# google-generativeai==0.8.5